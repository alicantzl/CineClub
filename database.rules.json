{
  "rules": {
    "rooms": {
      "$roomId": {
        "users": {
          ".read": "auth != null",
          ".write": "auth != null"
        },
        "reactions": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}